spring:
  datasource:
      driver-class-name: org.h2.Driver
      password: null
      url: jdbc:h2:mem:testdb
      username: sa   
  jpa:
    properties:
      hibernate:
      format-sql: true
      show_sql: true
  
server:
  port: 8686
  servlet:
    context-path: /SSA
     
    
      